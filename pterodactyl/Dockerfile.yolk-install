# Supertonic Voice Builder — Pterodactyl egg *install* image only (root).
# Runtime servers use supertonic-voice-builder-ptero:runtime (USER container).
ARG PT_RUNTIME_IMAGE=ghcr.io/scriptjunkiedev/supertonic-voice-builder-ptero:runtime
FROM ${PT_RUNTIME_IMAGE}

USER root
WORKDIR /mnt/server
