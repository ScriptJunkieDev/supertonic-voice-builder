# Pterodactyl *installation* image only (runs as root so Wings can execute /mnt/install/install.sh).
# Runtime server still uses java21-python-yolk:java_21_python (USER container).
FROM ghcr.io/scriptjunkiedev/java21-python-yolk:java_21_python

USER root
WORKDIR /mnt/server
