#!/usr/bin/env bash
# Deprecated: trainer setup is handled by the Spring Boot app at startup (TrainerBootstrapService).
# See TRAINER_GIT_URL, TRAINER_BACKUP_DIR, and trainer-backup/README.md.
echo "This script is no longer required. Start the app.jar; check GET /api/health for trainer bootstrap status."
exit 0
